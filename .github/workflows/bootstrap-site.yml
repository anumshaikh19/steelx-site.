name: Bootstrap site from archive

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  bootstrap:
    # Run only while the repository is still archive-only.
    if: ${{ !hashFiles('package.json') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Extract site
        shell: bash
        run: |
          set -euo pipefail
          test -f steelx-site.zip
          mkdir -p /tmp/steelx-site
          unzip -q steelx-site.zip -d /tmp/steelx-site

          # Copy the application source, but never copy local secrets or git metadata.
          rsync -a --delete \
            --exclude='.env' \
            --exclude='.env.*' \
            --exclude='.git/' \
            --exclude='node_modules/' \
            --exclude='dist/' \
            --exclude='dist-ssr/' \
            --exclude='.output/' \
            --exclude='.vinxi/' \
            --exclude='.nitro/' \
            --exclude='tsconfig.tsbuildinfo' \
            /tmp/steelx-site/ ./

          rm -f steelx-site.zip
          rm -f .github/workflows/bootstrap-site.yml

          # Prevent accidental future commits of local environment files.
          touch .env.example
          grep -qxF '.env' .gitignore || printf '\n# Local environment files\n.env\n.env.*\n!.env.example\n' >> .gitignore

      - name: Commit extracted application
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'chore: extract application source for Vercel deployment' || exit 0
          git push
